-- the SQL statement below computes the score average of all records in the 
-- table second_table of the database simba_group in the PostgreSQL Server.

SELECT AVG(score) AS average
FROM second_table;

